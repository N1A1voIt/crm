package site.easy.to.build.crm.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import site.easy.to.build.crm.entity.VExpensLead;

public interface VExpensLeadRepository extends JpaRepository<VExpensLead,Long> {
}
